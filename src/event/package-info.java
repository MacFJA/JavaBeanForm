/**
 * The package contains all event use in JavaBeanForm.
 * <hr>
 * Most of this event are internal and don't need to be handle.<br>
 * Expect for {@link event.UpdateEvent} if you create your own {@link ui.UI}.
 * And the {@link event.AddEvent} that you need to implement if you want to be able to add new object.
 */
package event;